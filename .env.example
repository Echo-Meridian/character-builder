# Sidonia Character Builder - Environment Configuration

# Worker URL for AI/RAG functionality
# Development: http://localhost:8787/api (run `cd worker && npx wrangler dev`)
# Production: https://sidonia-worker.<your-subdomain>.workers.dev/api
VITE_WORKER_URL=http://localhost:8787/api
